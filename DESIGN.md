# VizGen 设计决策与取舍（DESIGN.md）

本文档记录 VizGen 开发过程中的关键技术决策，重点说明任务如何拆解、技术选型依据、复杂度控制与取舍思路。

## 1. 为什么垂直化（看板工厂）而不是通用 App 生成器？

- 通用生成器要覆盖的表达空间过大，在有限时间内难以做深；垂直场景反而能做到"这个细分比通用模板更好用"
- LLM 输出面收窄为结构化配置后，**输出可校验、可兜底**——这是 6-8 小时内做到"演示零翻车"的唯一可靠路径
- 架构上 Builder 层与场景解耦：`engine.js` 的 `renderDashboardHTML(config, dataset)` 是唯一渲染出口。扩展到表单/页面生成场景时，只需替换配置 schema 与渲染模板，Agent 流水线（pipeline.js）完全复用

## 2. 为什么 LLM 只输出 JSON 配置，而不直接生成看板代码？

- 直接生成 HTML/CSS/JS：不可控（布局崩坏、脚本错误无法静态发现）、不可增量迭代（每次全量重写）
- LLM 输出配置 JSON：出错面收窄为"字段名写错、类型不匹配"等有限模式，Checker 能**穷尽校验**；修改请求是配置级 diff，天然支持增量
- 代价：表达能力受限于预设图表类型（kpi/bar/line/pie）。这是有意的取舍——原型阶段"稳定可迭代"比"表达力"更重要

## 3. 双通道降级设计

- 未配置 `LLM_API_KEY` 或 LLM 调用失败时，自动切换本地规则引擎：字段类型推断 → 默认图表组合；修改类请求用关键词+句式解析做增量变更
- 所有 Agent 步骤对用户透明标注引擎来源（"LLM 规划" / "规则引擎"徽章），不伪装成 LLM 输出
- **当前状态**：LLM 通道已用 DeepSeek（`deepseek-chat`，JSON 模式）实测通过，生成与修改两类请求均由 LLM 完成规划（响应中 `engine: "llm"`）；规则引擎退居降级通道
- **成本控制**：因"LLM 只输出配置 JSON、不写渲染代码"，单次生成仅消耗约 1-2K tokens，成本在厘级，可支撑大量演示调用

## 4. 生成应用的运行方式：iframe srcdoc 沙箱

- 生成物是**自包含单文件 HTML**（应用：样式/交互/localStorage 全内联；看板：数据内嵌、ECharts 走 CDN），用 `iframe.srcdoc` 渲染
- 对比方案：真编译成 Node 服务再运行——需要容器/进程隔离，安全面与复杂度都远超本项目范围；srcdoc 沙箱天然与主应用隔离，实现成本几乎为零
- 应用提示词明确禁止外部网络依赖（CDN/外链图片/fetch），保证生成的应用在沙箱内离线可用；看板的 ECharts 为唯一例外（渲染层确定性可控）
- 生成的应用自带真实交互（键盘/触屏操作、状态持久化、响应式），满足"真实交互而非静态展示"

## 5. Agent 步骤流式协议

- 生成接口用 NDJSON 流：每步产出 `{type:"steps", steps:[...]}` 快照，最终 `{type:"result", ...}`（V2 起含 `kind: "app"|"dashboard"`）
- 客户端逐行解析、就地更新步骤卡片——用户能实时看到 Agent 在做什么，而不是转圈等结果
- 选择 NDJSON 而非 SSE：生成是 POST 请求（携带需求文本），SSE 原生不支持 POST

## 6. 数据模型与持久化

- 五张表：users / projects / datasets / versions / messages
- 每次生成创建版本快照，支持版本切换与回看；消息表持久化完整对话与步骤记录，刷新后工作台可完整还原
- **V2 平滑迁移**：`projects` 增加 `kind` 列（app/dashboard）、`versions` 增加 `html_text`（应用 HTML 直接落库）与 `kind` 列、`dataset_id` 放开为可空（应用项目无数据集）；迁移用 PRAGMA 检查 + ALTER/重建，老数据自动补默认值，旧版本行为不变
- 选择 SQLite（node:sqlite 内置模块）：零原生编译依赖，Windows 一条 `npm start` 即可运行

## 7. 已知边界与扩展方向

- 应用模式质量依赖 LLM 输出（已通过校验+重试+模板兜底控制下限），复杂应用（多页面、真实后端）超出单文件沙箱范围
- 图表类型目前支持 kpi/bar/line/pie 四种（表格、散点、地图等可按同一配置协议扩展）
- 数据集上限 5000 行（内嵌 HTML 体积考量），更大数据需改为服务端聚合 API
- 迭代修改在规则引擎通道基于句式解析（"把X换成Y"），复杂指令依赖 LLM 通道
- 版本对比目前仅做配置级 diff 摘要，未做逐字符代码 diff

## 8. 线上环境问题定位记录

以下两个问题都**只在线上复现、本地完全正常**，是"可交付性"环节最消耗时间也最有价值的部分。

### 8.1 线上"注册成功但所有接口 401"

现象：线上注册/登录返回成功，但随后所有需要认证的请求（`/api/me`、`/api/projects`）一律 401；本地同代码完全正常。

定位过程：
1. 用 curl 隔离测试线上 API，确认服务端确实返回 401（排除前端问题）
2. 先怀疑凭据通道 → 改为 Cookie 传递并下发 `HttpOnly Cookie`，问题依旧
3. 兜底方案：在服务端临时记录被拒请求实际收到的请求头（采样日志），再用无头浏览器在线上复现一次 401，回读日志
4. **拿到铁证**：服务端收到的 `Authorization` 头内容是 `{"iss":"cloudstudio.net", ...}` —— **部署网关用自身平台 JWT 覆盖了我们的 Authorization 头**，自研 token 从未到达服务端；同时 Cookie 头原样到达
5. 另一个叠加问题：响应头里出现 `Eo-Cache-Status`（EdgeOne），说明 **CDN 把 GET 请求的 401 错误响应也缓存了**，导致修复前重试永远命中旧 401

修复（四层防护）：
- 认证提取优先级改为 **Cookie > 自定义头 `X-VizGen-Token` > Bearer**（Cookie 实测可穿透网关）
- 所有 `/api` 响应加 `Cache-Control: no-store, no-cache, must-revalidate`
- 前端 GET 请求追加时间戳参数穿透缓存
- 顺带修复：登录/注册 Tab 切换不再清空已输入内容

验证：线上端到端（注册 → 新建项目 → 导入数据 → Agent 生成看板）全流程通过，零 4xx/5xx。

**方法论沉淀**：本地正常、线上异常时，不要靠猜——让服务端把"它实际收到了什么"记录下来，用可复现的最小请求去触发，比反复改代码试错快得多。诊断端点与失败采样日志在问题解决后即移除。

### 8.2 受限网络下的代码托管（GitHub 推送）

现象：`git push` 反复失败——先是 `Connection was reset`，改走环境代理后 `CONNECT tunnel failed, response 502`，直连则完全超时。

定位过程：分别测试三条通道的连通性，发现规律：
- `github.com:443`（HTTPS 推送接口）：小请求偶通，一传数据就被重置
- 环境代理：拒绝 CONNECT 隧道（502）
- `api.github.com`：稳定 200
- **`ssh.github.com:443`（GitHub 官方 SSH 备用通道）：稳定连通**

解决：生成 ed25519 密钥并配置 `~/.ssh/config` 将 `github.com` 指向 `ssh.github.com:443`，远程地址改用 SSH，推送一次成功。备选方案用 `scripts/api-push.js`（GitHub REST API 逐文件上传，适用于仅 API 可达的环境）。

**方法论沉淀**：面对网络层阻断，先做"分通道连通性诊断"再动手，把不确定性收敛成一张通道矩阵，比反复重试高效得多。

## 9. V1 → V2 演进记录：从"看板工厂"到"通用应用工厂"

初版为何垂直化：通用生成器表达空间过大、LLM 输出不可控、演示易翻车，垂直看板场景能在有限时间内做到"输出可校验、可兜底"。

V2 为何放开为通用平台：初版架构预留了"Builder 层与场景解耦"的扩展路径——pipeline 只依赖"输入需求 → 产出可渲染 HTML"的抽象。放开为通用应用模式时：

1. **新增 `appgen.js`**：LLM 输出清洗（剥 markdown 围栏、截取 `<!DOCTYPE html>...</html>`）+ 完整性校验（结构标签、交互逻辑、拒绝话术检测）+ 标题提取
2. **新增 `templates.js`**：5 个高质量内置模板（计算器/待办/番茄钟/2048/记事本），关键词匹配，既是 LLM 不可用时的兜底，也是应用模式降级通道
3. **`llm.js` 扩展**：应用模式用纯文本生成（`chatText`，禁 JSON 模式），看板模式维持 JSON 模式——同一 LLM 通道两种输出协议
4. **`pipeline.js` 双模式分支**：应用模式五阶段（理解需求→设计方案→生成代码→校验完整性→修复/兜底→渲染），看板模式保持原有流程，步骤事件与结果协议完全一致
5. **存储迁移**：versions 表 HTML 直接落库（`html_text`），dataset_id 可空，preview/share 端点按 `kind` 分流——版本快照、公开分享、代码视图对两种模式零成本复用

**核心经验：先把"校验-修复-兜底"骨架做扎实，再扩展生成场景，新场景只是换一种 Builder 策略**——稳定性设计从看板模式平移到应用模式，几乎没有额外成本。
