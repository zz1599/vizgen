# VizGen 项目说明（实现思路 / 完成程度 / 扩展计划）

> 一句话：**VizGen 是一个 AI Agent 驱动的数据看板生成平台** —— 用户上传 CSV 并用自然语言描述需求，Agent 流水线自动生成一个可交互、可分享的数据看板网页应用，并在对话式工作台中实时预览与迭代。

- 在线体验：**https://vizgen.onrender.com**（Render 免费实例，15 分钟无访问会休眠，首次打开约需 30-60 秒唤醒）
- 演示账号：`demo@vizgen.dev` / `demo1234`（已预置成品看板，打开即可看效果）
- 源码：**https://github.com/zz1599/vizgen**（Public）
- 本地运行：`npm install && npm start`（Node 18+，详见 README.md）

## 1. 实现思路与关键取舍

**产品定位：不做通用应用生成器，做垂直化的"看板工厂"。** 通用生成器在表达力上很难做好，垂直场景里 LLM 输出可控、生成结果可客观校验，能在有限时间内做到"这个细分比模板工具更好用"。架构上 Builder 层与场景解耦（LLM 提示词 + 渲染引擎可替换），扩展到表单、其他可视化等场景有明确路径。

三个最关键的取舍：

1. **LLM 只做决策，不写渲染代码。** LLM 输出结构化 JSON 配置（图表类型 + 字段映射 + 聚合方式），看板 HTML 由确定性模板引擎渲染。收益：生成结果永远可解析、可校验、可 diff，"对话式增量修改"从概率问题变成结构化字段修改问题。
2. **双通道降级设计。** LLM 不可用（未配置 Key 或调用失败）时自动切换规则引擎（字段类型 → 默认图表组合，关键词 → 增量修改），两条通道共享同一套 Checker 校验与 HTML 引擎。收益：生成能力始终可用，不会因外部依赖波动而中断。
3. **生成产物 = 自包含单文件 HTML + iframe srcdoc 沙箱。** 不做容器编译/Node 运行时，即可完成"生成→预览→迭代→分享"闭环，且天然安全隔离、分享链接零成本（存版本快照，`/s/:id` 只读渲染）。

**核心亮点：Agent 自检修复闭环。** Planner→Builder→Checker→Fixer→Render 流水线中，Checker 做的是**真验证**（图表引用的字段是否存在、类型是否匹配、聚合是否合法），失败则携带错误信息由 Fixer 自动修复——全程在 UI 上以步骤流展示，用户能看到 Agent 每一步在做什么。

**AI 工具链工作流**：本项目由 AI 辅助编码工具协作完成——需求拆解与方案设计 → 逐模块生成（CSV 解析 / 规则引擎 / 流水线 / 前端 SPA）→ curl 全链路冒烟 + 无头浏览器 UI 冒烟验证 → 部署后线上问题定位修复。开发中的两个真实案例（详见 DESIGN.md 第 8 节）：**①** 线上所有认证请求 401 而本地正常，通过服务端失败采样日志定位到部署网关会覆盖 `Authorization` 头，改用 Cookie 优先的认证通道解决；**②** `git push` 在受限网络下持续失败，通过分通道连通性诊断定位到只有 `ssh.github.com:443` 稳定可用，改走 SSH 备用通道推送成功。

## 2. 当前完成程度

**已实现（全部可用，非 PoC）：**

- ✅ 完整账号体系：注册 / 登录（scrypt + HMAC Token + HttpOnly Cookie）
- ✅ 数据导入：CSV 上传 / 粘贴 / 内置示例，字段类型自动推断
- ✅ Agent 流水线生成：五阶段步骤流式展示 + 自检修复闭环
- ✅ 对话式迭代：改图表类型 / 标题 / 增删图表，增量生效
- ✅ 实时预览：iframe 沙箱渲染，桌面 / 移动视图切换
- ✅ 版本管理：每次生成自动快照，可切换历史版本
- ✅ 公开分享：每版本一个只读链接，无需登录
- ✅ 代码 / 配置视图：可查看生成应用真实源码与配置 JSON（含变更摘要）
- ✅ 数据持久化：SQLite 全量存储（用户 / 项目 / 数据集 / 版本 / 消息）
- ✅ 质量验证：curl 全链路冒烟 + 无头浏览器 UI 冒烟脚本，均通过

**未实现（有意取舍）：**

- ❌ 模板 / 作品广场与 Remix（低优先级，与核心闭环无关）
- ❌ 团队协作 / 多人编辑（超出本项目范围）
- ❌ 生成应用的导出下载（快照已存，接口易补）

**LLM 通道状态**：已配置 DeepSeek（`deepseek-chat`）并实测通过——生成请求（"生成一个销售数据看板，突出总销售额和区域表现"）与修改请求（"把饼图删掉，再加一个按商品维度的销售额排行条形图"）均由 LLM 完成规划，响应标记 `engine: "llm"`，Agent 步骤中显示"LLM 规划"徽章。单次生成仅消耗约 1-2K tokens（LLM 只输出配置 JSON），成本在厘级。规则引擎作为降级通道保留，两者共用同一套 Checker 校验与渲染引擎。

## 3. 如果继续投入时间：扩展方向与优先级

| 优先级 | 方向 | 理由 |
|---|---|---|
| P0 | **LLM 通道实测调优**：真实 LLM 输出的配置质量评估、Few-shot 优化、失败率统计 | 双通道架构已就绪，这是从"可演示"到"生产可用"的最短路径 |
| P1 | **数据源扩展**：Excel / Google Sheets / API 拉取，及数据集更新后看板一键刷新 | 看板场景的真实用户需求，复用现有 CSV 层即可 |
| P1 | **生成应用交互持久化**：用户在看板上的筛选/视图状态保存为"个人视图" | 延展能力与云端能力叙事双赢 |
| P2 | **修复对话增强**：多轮上下文累积、修改预览（diff 确认后再应用） | 提升迭代体验的确定性 |
| P2 | **导出与嵌入**：下载 HTML / iframe 嵌入代码 / PNG 截图 | 成本低、演示效果好 |
| P3 | 模板广场与 Remix、团队协作 | 产品化阶段再考虑 |

## 4. 技术文档索引

- `README.md` —— 架构、功能清单、快速开始、项目结构
- `DESIGN.md` —— 8 节关键设计决策与取舍（含线上问题定位记录）
