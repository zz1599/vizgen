# VizGen 项目说明（实现思路 / 完成程度 / 扩展计划）

> 一句话：**VizGen 是一个 AI Agent 驱动的通用应用生成平台（AI vibe coding 工厂）** —— 用户用自然语言描述想要的 Web 应用（计算器、2048 游戏、番茄钟、数据看板……），Agent 流水线自动生成一个可交互、可分享的网页应用，并在对话式工作台中实时预览与迭代。

- 在线体验：**https://vizgen.onrender.com**（Render 免费实例，15 分钟无访问会休眠，首次打开约需 30-60 秒唤醒）
- 演示账号：`demo@vizgen.dev` / `demo1234`（已预置销售看板与 2048 小游戏两个示例项目）
- 源码：**https://github.com/zz1599/vizgen**（Public）
- 本地运行：`npm install && npm start`（Node 18+，详见 README.md）

## 1. 实现思路与关键取舍

**产品定位：通用 vibe coding 平台，支持"任意应用 + 数据看板"双模式。** 用户输入一句话描述想生成的应用——不限定场景：计算器、2048 游戏、待办清单、番茄钟、记事本等任意 Web 小应用，或上传 CSV 生成数据看板。Agent 流水线对两种模式复用同一套"生成 → 校验 → 修复 → 兜底"骨架，只在 Builder 层按场景切换策略。

三个最关键的取舍：

1. **按场景选择生成策略：应用让 LLM 直接写代码，看板让 LLM 只输出配置。** 应用模式（计算器/游戏/工具）表达空间大，由 LLM 直接生成完整单文件 HTML（样式/交互/localStorage 持久化全内联，禁外部依赖保证沙箱可用）；看板模式对数据正确性要求高，LLM 只输出结构化 JSON 配置（图表类型 + 字段映射 + 聚合），HTML 由确定性引擎渲染。各自拿到"表达力 vs 可控性"的最优解。
2. **Checker 真验证 + 携带错误反馈重试 + 模板兜底，生成能力永不中断。** 应用模式校验 HTML 结构完整性与交互逻辑存在性，失败把错误清单喂回 LLM 重试一次，仍失败则降级到内置同款模板（计算器/待办/番茄钟/2048/记事本，关键词匹配）；看板模式校验字段存在性与类型匹配，失败自动修复剔除。LLM 不可用（未配置 Key 或调用失败）时整条链路走内置模板/规则引擎。
3. **生成产物 = 自包含单文件 HTML + iframe srcdoc 沙箱。** 不做容器编译/Node 运行时，即可完成"生成→预览→迭代→分享"闭环，且天然安全隔离、分享链接零成本（存版本快照，`/s/:id` 只读渲染）。

**核心亮点：Agent 自检修复闭环。** Planner→Builder→Checker→Fixer→Render 流水线中，Checker 做的是**真验证**（应用：HTML 结构/交互逻辑；看板：字段引用/类型/聚合），失败则携带错误信息由 Fixer 自动修复——全程在 UI 上以步骤流展示，用户能看到 Agent 每一步在做什么。

**AI 工具链工作流**：本项目由 AI 辅助编码工具协作完成——需求拆解与方案设计 → 逐模块生成（CSV 解析 / 规则引擎 / 流水线 / 前端 SPA）→ curl 全链路冒烟 + 无头浏览器 UI 冒烟验证 → 部署后线上问题定位修复。开发中的两个真实案例（详见 DESIGN.md 第 8 节）：**①** 线上所有认证请求 401 而本地正常，通过服务端失败采样日志定位到部署网关会覆盖 `Authorization` 头，改用 Cookie 优先的认证通道解决；**②** `git push` 在受限网络下持续失败，通过分通道连通性诊断定位到只有 `ssh.github.com:443` 稳定可用，改走 SSH 备用通道推送成功。

## 2. 当前完成程度

**已实现（全部可用，非 PoC）：**

- ✅ 完整账号体系：注册 / 登录（scrypt + HMAC Token + HttpOnly Cookie）
- ✅ **AI 应用模式**：一句话生成任意单文件 Web 应用（计算器 / 2048 / 待办 / 番茄钟 / 记事本 / 记账本…），LLM 生成完整 HTML，校验失败自动修复，最终兜底内置模板；对话式迭代（"改成深色主题"、"加一个统计功能"）
- ✅ **数据看板模式**：CSV 上传 / 粘贴 / 内置示例（字段类型自动推断）→ LLM 规划图表配置 → ECharts 渲染；对话式增删改图表
- ✅ Agent 流水线生成：五阶段步骤流式展示 + 自检修复闭环（双模式各自的真实校验）
- ✅ 实时预览：iframe 沙箱渲染，桌面 / 移动视图切换
- ✅ 版本管理：每次生成自动快照，可切换历史版本
- ✅ 公开分享：每版本一个只读链接，无需登录
- ✅ 代码视图：可查看生成应用真实源码
- ✅ 数据持久化：SQLite 全量存储（用户 / 项目 / 数据集 / 版本 / 消息），含平滑版本迁移
- ✅ 质量验证：curl 全链路冒烟（应用生成/修改、看板生成/迭代）+ 无头浏览器 UI 冒烟脚本，均通过

**未实现（有意取舍）：**

- ❌ 模板 / 作品广场与 Remix（低优先级，与核心闭环无关）
- ❌ 团队协作 / 多人编辑（超出本项目范围）
- ❌ 生成应用的导出下载（快照已存，接口易补）

**LLM 通道状态**：已配置 DeepSeek（`deepseek-chat`）并实测通过。应用模式："做一个计算器，支持加减乘除和键盘输入" 生成 12 KB 可运行应用，"把界面配色改成深色主题" 增量修改成功；看板模式："生成一个销售数据看板" 规划 7 个图表（KPI/折线/柱状/饼图），字段映射全部正确。响应标记 `engine: "llm"`，Agent 步骤中显示"LLM 规划"徽章。应用模式单次约 5-8K tokens（输出完整 HTML），看板模式约 1-2K tokens（只输出配置 JSON）。

## 3. 如果继续投入时间：扩展方向与优先级

| 优先级 | 方向 | 理由 |
|---|---|---|
| P0 | **应用模式生成质量调优**：多模板扩充、Few-shot、移动端适配约束、失败率统计 | 应用模式是主入口，质量决定口碑 |
| P1 | **数据源扩展**：Excel / Google Sheets / API 拉取，及数据集更新后看板一键刷新 | 看板场景的真实用户需求，复用现有 CSV 层即可 |
| P1 | **生成应用交互持久化**：用户在应用/看板上的状态保存为"个人视图" | 延展能力与云端能力叙事双赢 |
| P2 | **修复对话增强**：多轮上下文累积、修改预览（diff 确认后再应用） | 提升迭代体验的确定性 |
| P2 | **导出与嵌入**：下载 HTML / iframe 嵌入代码 / PNG 截图 | 成本低、演示效果好 |
| P3 | 模板广场与 Remix、团队协作 | 产品化阶段再考虑 |

## 4. 技术文档索引

- `README.md` —— 架构、功能清单、快速开始、项目结构
- `DESIGN.md` —— 8 节关键设计决策与取舍（含线上问题定位记录）
